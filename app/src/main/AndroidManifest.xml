<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.behoctoan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Tinh.level3"></activity>
        <activity android:name=".Tinh.level2" />
        <activity android:name=".Tinh.level1" />
        <activity android:name=".tap_dem" />
        <activity android:name=".so_sanh" />
        <activity android:name=".Tinh.Tinh_nham" />
        <activity android:name=".Xem_diem" />
        <activity android:name=".Kiem_tra" />
        <activity android:name=".Luyen_tap" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>